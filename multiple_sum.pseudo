create a variable `counter` equal to 1000
create a variable `sum` equal to 0
create a while-loop until `counter` is equal to 0
    check if counter mod 3 is 0
        if so:
          add to sum
        otherwise:
          check if counter mod 5 is 0
            if so:
               add to sum
    subtract counter by 1
print sum